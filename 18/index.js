const test = require('./test');

//console.log(printName('Adrian'), PI);// to run this, we need to call its object 1st as the arg contained method and property at below

console.log(test.printName('Adrian'), test.PI); //type node .

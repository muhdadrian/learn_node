const test = require('./test');

console.log(
  test.printName('Adrian'),
  test.PI,
  test.student.printStudent(),
  new test.Person()
);

//we did learn Local Modules until here. Next we'll learn Core Modules.

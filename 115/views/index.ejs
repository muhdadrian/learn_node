<!-- the code below will go to the body in div container (main-layout) as long as we modify the route in index in app.js -->
    <h1>Web Server NodeJS</h1>
    <h2>Hello, <%= name %></h2>

    <h4>Students Registration</h4>

    <% if (students.length < 1) { %>
    <p>Students data is empty</p>
    <% } else {%> <% students.forEach(student => { %>
    <ul>
      <li>Name : <%= student.name %></li>
      <li>Email : <%= student.email %></li>
    </ul>
    <% }) %> <% } %>

const name = 'Adrian';
const printName = name => `Hi, my name is ${name}`;
console.log(printName(name)); // type 'node index' in VSC terminal

//the name in line 3 is using the var in line 1 as an arg. The name in line two is an param

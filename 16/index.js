const printName = require('./test');
const PI = require('./test');

console.log(printName('Adrian'), PI);
//console.log(PI);

//when you type node . it will produce error. But, if you comment the line 1 and 4, while uncommenting the line 5. It will run.

//we want to run both. Go to 17

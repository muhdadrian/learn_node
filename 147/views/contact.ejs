<div class="container mt-3" >
	<div class="row">
		<div class="col-md-8">
			<h2>Contact Registration</h2>
				<a href="/contact/add" class="btn btn-primary mb-2">Add Contact Data</a>

				<!-- 1st, we check here. If the msg has a value we'll displayed green msg (ejsif).   -->
				 <% if (msg) { %>
					<div class="alert alert-success" role="alert"><%= msg %></div>
				 <% } %>
				 <!-- 2nd, refresh the contact page in browser, the green block will be displayed. The msg in round bracket above has a value (empty str) even it's empty. Go to 148 for next action by adding a length -->
			<table class="table">
				<thead>
					<tr class="table-primary">
						<th scope="col">#</th>
						<th scope="col">Name</th>
						<th scope="col">Phone No.</th>
						<th scope="col">Action</th>
					</tr>
				</thead>
				 <% if (contacts.length === 0) { %>
					<tr>
						<td colspan="4">
							<div class="alert alert-danger fw-bold" role="alert">
								The contact data is empty! 
							</div>
						</td>
					</tr>
				 <% } %>
				<tbody>
					 <% let no = 1; %>
						<% contacts.forEach((contact, i) => { i++ %>
							<tr>
								<th scope="row"><%= i++ %></th> 
								<td><%= contact.name %></td>
								<td><%= contact.phoneno %></td>
								<td>						
									<a href="/contact/<%= contact.name  %>" class="btn btn-success badge rounded-pill"><i class="bi bi-info-circle"></i> detail</a>
								</td>					
							</tr>
						<% }) %>
				</tbody>
			</table>
		</div>
	</div>
</div>
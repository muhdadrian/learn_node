console.log(window.printName('Adrian')); //this will also run even you delete the window keyword. That's why to make JS in browser, you must be careful of not using similar name for the vars and functions. What else if you are using external library like jQuery -- cannot be similar for the var and function name.

//But in Node JS we cannot call the file as global object. Go to 11
